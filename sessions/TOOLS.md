# Журнал инструментов

> Только дополняется, старые записи не редактируются. Сюда попадает всё, что вы
> добавили в проект или окружение ради этой задачи: библиотеки, CLI-утилиты,
> линтеры, MCP-серверы, плагины и скиллы агента, хуки, внешние сервисы, контейнеры.
>
> Разовый вызов уже установленного инструмента сюда не пишется — он идёт
> в таблицу инструментов внутри файла сессии.
>
> Тупиковая попытка — тоже запись: поставили, не подошло, сняли. Это как раз интересно.

## Шаблон записи

```markdown
## YYYY-MM-DD · Сессия N · <инструмент> vX.Y

- **Тип:** библиотека / CLI / MCP / плагин / скилл / хук / сервис
- **Установка:** `<точная команда>`
- **Зачем:** какую задачу решает
- **Область:** проект или глобально
- **Проверка:** чем убедились, что работает
```

Удалённый или заменённый инструмент фиксируется так же — с пометкой
**удалён** / **заменён на X** и причиной.

---

## 2026-08-16 · Сессия 1 · GitHub-репозиторий FMarsM/AI-homework

- **Тип:** сервис
- **Установка:** `git init`; `git remote add origin https://github.com/FMarsM/AI-homework.git`; `git branch -M main`; `git push -u origin main`
- **Зачем:** хранение и сдача домашнего задания — публичный репозиторий, куда сдаётся код и журнал сессий.
- **Область:** проект
- **Проверка:** `git ls-remote` до пуша (репозиторий пуст, но доступен), после пуша — коммит виден по ссылке https://github.com/FMarsM/AI-homework/commit/31c093c

## 2026-08-16 · Сессия 2 · GitHub CLI (gh) — рассмотрен, не установлен

- **Тип:** CLI
- **Установка:** не выполнялась
- **Зачем:** планировался для `gh pr create`, чтобы открыть pull request на изменения сессии
- **Область:** проект
- **Проверка:** `gh --version` — `command not found` и в Bash, и в PowerShell. Ставить не стали: решили коммитить и пушить изменения прямо в `main` без PR, отдельной feature-ветки в проекте пока нет.

## 2026-08-17 · Сессия 4 · @playwright/test v1.62.1

- **Тип:** библиотека
- **Установка:** `npm init -y`; `npm install -D @playwright/test`; `npx playwright install chromium` — всё в `qa/playwright/`
- **Зачем:** автотесты на найденные дефекты сайта (падающий тест как доказательство бага), скриншоты на ширинах 375 / 768 / 1024 / 1440, перехват запросов к боевому Google Apps Script вместо реальных отправок формы
- **Область:** проект (`qa/playwright/`, не глобально)
- **Проверка:** `npx playwright --version` → `Version 1.62.1`; бандл браузера — `Chromium 151.0.7922.34`. Прогон `evidence.spec.js` → `13 passed (19.3s)`, прогон `bugs.spec.js` → `8 failed` (падения ожидаемые, на дефектах сайта)

> Запись из `STATE.md` сессии 2 «Node.js/npm/npx — не установлены» на этой машине больше
> не соответствует действительности: `node v24.19.0`, `npm 11.17.0` присутствуют,
> устанавливать Node не потребовалось.

## 2026-08-17 · Сессия 4 · Встроенный Browser MCP — частично не подошёл

- **Тип:** MCP
- **Установка:** не требуется, встроен в агента
- **Зачем:** планировался как основной инструмент ручного прогона и скриншотов (так записано в `AGENTS.md`, «Стек и версии»)
- **Область:** проект
- **Проверка:** `read_page`, `javascript_tool`, `resize_window`, `navigate` работают — через них собраны ресурсы и ссылки по всем 10 страницам и вычисленные стили. А `computer` с действием `left_click` стабильно падает: `computer timed out after 30s. The Browser pane is currently hidden`. **Тупик для интерактивных шагов:** клики (гамбургер-меню, клик мимо меню, кнопка отправки) и файлы-скриншоты перенесены в Playwright. Инструмент не снят — оставлен для осмотра страниц и запуска проверочных скриптов.

## 2026-08-17 · Сессия 5 · Вторая установка @playwright/test — рассмотрена, не выполнена

- **Тип:** библиотека
- **Установка:** не выполнялась
- **Зачем:** для независимого аудита требовалось перепрогнать `bugs.spec.js` и `evidence.spec.js`, не затронув `qa/`. Прямой запуск не годится: спеки пишут скриншоты в `qa/screenshots/` с фиксированными именами и затирают сохранённые доказательства к багам, что запрещено `AGENTS.md` («Что нельзя трогать»). Первый вариант — поднять отдельную установку Playwright в scratchpad.
- **Область:** проект
- **Проверка:** от установки отказались — `AGENTS.md` требует спрашивать человека перед добавлением зависимостей, а тут та же зависимость встала бы вторым экземпляром ради одного прогона. Вместо этого спек-файлы скопированы в scratchpad, путь скриншотов в копиях переписан на локальный каталог, а `node_modules` подключён к существующей установке ссылкой: `New-Item -ItemType Junction -Path <scratchpad>\audit-run\node_modules -Target C:\ClaudeHomework\TestTask\qa\playwright\node_modules`. Сработало: `bugs.spec.js` → `8 failed`, `evidence.spec.js` → `13 passed (20.0s)`, файлы в `qa/screenshots/` не изменились. Приём годится для любого повторного прогона, пока не исправлена находка B-6 (жёсткие пути скриншотов внутри спеков).

# Журнал инструментов

> Только дополняется, старые записи не редактируются. Сюда попадает всё, что вы
> добавили в проект или окружение ради этой задачи: библиотеки, CLI-утилиты,
> линтеры, MCP-серверы, плагины и скиллы агента, хуки, внешние сервисы, контейнеры.
>
> Разовый вызов уже установленного инструмента сюда не пишется — он идёт
> в таблицу инструментов внутри файла сессии.
>
> Тупиковая попытка — тоже запись: поставили, не подошло, сняли. Это как раз интересно.

## Шаблон записи

```markdown
## YYYY-MM-DD · Сессия N · <инструмент> vX.Y

- **Тип:** библиотека / CLI / MCP / плагин / скилл / хук / сервис
- **Установка:** `<точная команда>`
- **Зачем:** какую задачу решает
- **Область:** проект или глобально
- **Проверка:** чем убедились, что работает
```

Удалённый или заменённый инструмент фиксируется так же — с пометкой
**удалён** / **заменён на X** и причиной.

---

## 2026-08-16 · Сессия 1 · GitHub-репозиторий FMarsM/AI-homework

- **Тип:** сервис
- **Установка:** `git init`; `git remote add origin https://github.com/FMarsM/AI-homework.git`; `git branch -M main`; `git push -u origin main`
- **Зачем:** хранение и сдача домашнего задания — публичный репозиторий, куда сдаётся код и журнал сессий.
- **Область:** проект
- **Проверка:** `git ls-remote` до пуша (репозиторий пуст, но доступен), после пуша — коммит виден по ссылке https://github.com/FMarsM/AI-homework/commit/31c093c

## 2026-08-16 · Сессия 2 · GitHub CLI (gh) — рассмотрен, не установлен

- **Тип:** CLI
- **Установка:** не выполнялась
- **Зачем:** планировался для `gh pr create`, чтобы открыть pull request на изменения сессии
- **Область:** проект
- **Проверка:** `gh --version` — `command not found` и в Bash, и в PowerShell. Ставить не стали: решили коммитить и пушить изменения прямо в `main` без PR, отдельной feature-ветки в проекте пока нет.

## 2026-08-16 · Сессия 3 · Node.js v24.19.0 — уже был в системе, не устанавливался

- **Тип:** CLI / среда выполнения
- **Установка:** не выполнялась
- **Зачем:** нужен для `npm` и `@playwright/test`
- **Область:** глобально (система)
- **Проверка:** первая попытка `npm install` упала с кодом 127 — `node` и `npm`
  не найдены ни в Bash, ни в PowerShell. Поиск по диску
  (`Get-ChildItem -Path C:\ -Filter node.exe -Recurse -Depth 3`) нашёл
  `C:\Program Files\nodejs\node.exe`. То есть **Node.js в системе есть, но его
  каталога нет в `PATH` этих оболочек** — это исправление к записи в `STATE.md`
  сессии 2, где Node.js был помечен как отсутствующий. Обходной путь: префикс
  `export PATH="/c/Program Files/nodejs:$PATH"` перед каждой командой.
  После этого `node --version` → `v24.19.0`, `npm --version` → `11.17.0`.

## 2026-08-16 · Сессия 3 · @playwright/test v1.62.1

- **Тип:** библиотека (dev-зависимость)
- **Установка:**
  ```
  export PATH="/c/Program Files/nodejs:$PATH"
  cd 3rd-attempt/playwright
  npm init -y
  npm install -D @playwright/test
  npx playwright install chromium
  ```
- **Зачем:** воспроизводимые автотесты на найденные дефекты и снятие скриншотов
  на четырёх ширинах. Ключевое для этой задачи — `page.route`: он перехватывает
  запросы к боевому эндпоинту Google Apps Script, поэтому форму бронирования
  можно тестировать, не отправляя владельцу сайта ни одной реальной заявки.
- **Область:** проект, каталог `3rd-attempt/playwright` (не в корне репозитория —
  чтобы не смешивать со сборкой тестируемого сайта)
- **Проверка:** `npx playwright --version` → `Version 1.62.1`;
  `npx playwright test --reporter=list` → `8 failed, 5 passed (24.2s)` —
  падения ожидаемые, это и есть зафиксированные дефекты.
  Браузер: `chromium.launch()` сообщает `Chromium 151.0.7922.34`.

## 2026-08-16 · Сессия 3 · Браузеры Playwright (Chromium 151.0.7922.34)

- **Тип:** внешний бинарник
- **Установка:** `npx playwright install chromium` — команда отработала
  без вывода
- **Область:** глобально, кэш `%LOCALAPPDATA%\ms-playwright`
- **Зачем:** движок для прогона автотестов
- **Проверка:** вывода у команды не было, потому что браузеры уже лежали
  в глобальном кэше от предыдущей попытки (папка `2nd-attempt`, удалённая
  в начале сессии). Кэш находится вне проекта, поэтому удаление `2nd-attempt`
  его не задело: `ls %LOCALAPPDATA%\ms-playwright` → `chromium-1234`,
  `chromium_headless_shell-1234`, `ffmpeg-1011`, `winldd-1007`.
  Повторная загрузка ~150 МБ не потребовалась.

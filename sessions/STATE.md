# Состояние проекта

> Обновляется в конце каждой сессии. Описывает «как есть сейчас», а не историю
> изменений — старое перезаписывается. Первым делом в новой сессии агент читает
> именно этот файл.

**Обновлено:** `2026-08-17, сессия 7`

## Готово

- `SPEC.md` — периметр QA-тестирования, сценарии, состояния, допущения, критерии приёмки.
- `AGENTS.md`, Часть II — стек, команды запуска, запреты, формат баг-репорта (8 пунктов),
  структура `qa/`.
- **Тест-план `qa/test-plan.md`** — 12 проверок (TC-01…TC-12), сохранён до начала прогона.
  Негативных/граничных фактически **8**, хотя в тексте плана написано «7 из 12» —
  расхождение зафиксировано аудитом (C-1). Требование «не меньше трети» выполнено в любом случае.
- **Прогон выполнен** на ширинах 375 / 768 / 1024 / 1440, скриншот на каждую ширину есть
  (`index` и `contacts` × 4 ширины).
- **`qa/bug-reports.md`** — 9 баг-репортов по обязательному составу из 8 пунктов,
  плюс 8 наблюдений (Н-1…Н-8), явно помеченных как *не* баг-репорты, плюс раздел
  «что проверялось и оказалось исправным».
- **Playwright установлен и работает** в `qa/playwright/` (`@playwright/test` 1.62.1,
  Chromium 151.0.7922.34): `evidence.spec.js` — 13 зелёных, `bugs.spec.js` — 8 красных.
- Реальных отправок формы бронирования израсходовано **1 из 10** (ФИО `TEST QA 2026-08-17`).
  Заявка не дошла: эндпоинт ответил 403.
- **Независимый аудит артефактов (сессия 5)** — `qa/independent-audit.md`. Прогон
  автотестов и причины багов перепроверены с нуля в чистой сессии: все 9 багов реальны,
  8 падений и 13 успехов воспроизводятся, цитаты в отчёте не приукрашены. Этим закрыто
  правило курса «Независимый взгляд» — до сессии 5 оно не было закрыто ни разу.
- **Баг 5 исправлен, повторная проверка проведена (сессия 6)** — по явному разрешению
  человека правились `contacts.html` и `contacts-eng.html` (проверка пары дат через
  `min` + `setCustomValidity`, +17 строк в каждый файл). Тот же `bugs.spec.js` без правок
  в нём даёт `7 failed, 1 passed` вместо `8 failed`, `evidence.spec.js` — 13 зелёных,
  консоль на правленых страницах чистая. Отчёт — `qa/retest-bug-05.md`, автотест —
  `qa/playwright/retest-bug-05.spec.js` (5 из 5). Критерий приёмки `SPEC.md` «повторная
  проверка после фикса хотя бы одного бага» **закрыт**.
- **Сравнение наивных прогонов с прогоном по правилам (сессия 7)** —
  `qa/naive-vs-rules-comparison.md`. Три набора артефактов (`3rd-attempt`, `4th-attempt`
  с ветки `naive-run` и `qa/`) сведены по формату баг-репорта, критериям приёмки,
  соглашениям проекта и покрытию дефектов. Сайт не запускался и не правился,
  отправок формы не было.

### Поправка к прошлой версии этого файла

Предыдущая редакция утверждала, что `session-4.md` заполнен «включая таблицу расхождений
„отчитались готово / проверка опровергла" (6 записей)» и содержит раздел «Осталось спросить».
**Ни того, ни другого в файле нет** — ни в рабочей копии, ни в коммите `a7fdf5f`.
Утверждение было неверным; критерий приёмки `SPEC.md` по расхождениям **не закрыт**.

## В работе

Ничего не находится в промежуточном состоянии. Сессии 4, 5, 6 и 7 закрыты.
Список находок аудита (A-1…C-5) сформирован, но к исправлению не приступали.

## Не начато

- **Устранение находок аудита.** Ни одна из 14 находок `qa/independent-audit.md` не закрыта.
  Приоритет задан в разделе «Порядок исправления» того же файла.
- **Фикс остальных 8 багов сайта.** Исправлен только баг 5. Правки
  `fmarsm.github.io-main/` требуют явного разрешения человека на каждый заход —
  по умолчанию в сессии тестирования они запрещены (`AGENTS.md`).
- **Сценарий 10 из `SPEC.md`** — обход всех интерактивных элементов клавиатурой.
  Не влез в лимит 12 проверок; наблюдения Н-5, Н-6, Н-7 указывают, что дефекты там есть.
- **Обновление `qa/bug-reports.md`** — баг 5 остаётся описанным как открытый, отметки
  «исправлен в сессии 6, см. `retest-bug-05.md`» в нём нет.
- **Две дыры в покрытии, вскрытые сравнением (сессия 7).** Обе подтверждены по исходнику,
  но не задокументированы как находки прогона:
  - поле «Номер телефона» (`contacts.html:65`, `type="tel"` без `pattern`) не проверяется
    ни одной из 12 проверок тест-плана и не упомянуто в баг-репортах — при том что оба
    наивных прогона его нашли;
  - опечатка `index-eng.html:101` («Where to fing us») не попала ни в баг-репорты,
    ни в наблюдения Н-1…Н-8.
- Коммит результатов сессии 7 — не закоммичены `qa/naive-vs-rules-comparison.md`,
  `sessions/session-7.md`, этот файл, а также правки человека в `session-5.md`
  и `session-6.md`. Сессия 6 закоммичена ранее (`8fd30f6`).

## Активные инструменты

- Python 3.13.2 — локальный сервер `python -m http.server 8080 --directory fmarsm.github.io-main`.
- Node.js v24.19.0 + npm 11.17.0 — присутствуют в системе, отдельно не устанавливались.
- `@playwright/test` 1.62.1 + Chromium 151.0.7922.34 — в `qa/playwright/`, установлен в сессии 4.
- Git — репозиторий `FMarsM/AI-homework`. Текущая ветка `main`; ветки `1st-attempt`,
  `2nd-attempt`, `naive-run` содержат материалы предыдущих подходов.
- Встроенный Browser MCP — используется для осмотра страниц и запуска скриптов;
  для кликов **не годится** (см. `TOOLS.md`).
- Приём для повторных прогонов без порчи доказательств: копия спеков в отдельном каталоге
  + junction на `qa/playwright/node_modules` (`New-Item -ItemType Junction`). Второй установки
  Playwright не потребовалось, запись в `TOOLS.md` от 2026-08-17.

## Известные проблемы

### Дыры в самих QA-артефактах (из `qa/independent-audit.md`)

- **A-1. Списка расхождений «отчитались готово / проверка опровергла» не существует.**
  Обязателен по `SPEC.md` и `AGENTS.md`. `qa/bug-reports.md:346-348` ссылается на него
  как на источник разбора главной гипотезы — ссылка ведёт в никуда.
- **A-2. Нет артефакта по TC-02** (network/console-лог по 10 страницам). Вывод «обратные
  слэши — не баг» **верен** и перепроверен независимо (53 уникальных ресурса, 0 не-200,
  браузер нормализует `\` → `/`), но число «152 ресурса» из отчёта ничем не подкреплено.
- **A-3. Нет реестра результатов** pass/fail по 12 проверкам, хотя тест-план его требует.
  Чек-боксы в `test-plan.md` и `SPEC.md` не отмечены.
- **B-1.** Три несогласованные нумерации: ID багов 1–9, автотесты `BUG-01…BUG-08`,
  имена скриншотов. `bug-01-*.png` — доказательство к багу №2.
- **B-2.** `bug-05-footer-dead-link.png` и `bug-04-eng-logo-href.png` дефект не показывают —
  `screenshot()` вызывается до действия, вскрывающего баг.
- **B-3.** Баги 2–5 задокументированы только для `contacts.html`, хотя `contacts-eng.html`
  содержит те же дефекты — область занижена вдвое.
- **B-4.** Баг 1 (403, Blocker) стоит на границе исключения `SPEC.md` про сторонние сервисы,
  причина не установлена; вопрос владельцу не задан.
- **B-5.** TC-02, TC-04, TC-05, TC-08, TC-09 покрыты уже, чем обещал план.
- **B-6.** `npx playwright test` перезаписывает 8 сохранённых скриншотов-доказательств —
  прямо против запрета в `AGENTS.md`. До исправления повторные прогоны делать только на копиях.
- **C-4.** В `sessions/session-4.md` пуст раздел «Финальный вердикт», есть пустой заголовок
  «Промпт 4», порядок разделов не совпадает с шаблоном `AGENTS.md`.

### Открытые вопросы к человеку

Раздел «Осталось спросить» в `session-4.md` отсутствует, поэтому вопросы фиксируются здесь,
чтобы не потерялись:

1. Обязательно ли замыкание цикла стрелок слайдера (баг 9) — или одной стрелки на крайних
   слайдах достаточно?
2. Считать ли дефектом `href="#"` в футере самой `contacts.html` (баг 8) — ссылка на
   страницу, на которой уже находишься?
3. Легально ли значение `0` в поле «Количество заселяющихся» (баг 4)?
4. Добавлено аудитом: остаётся ли баг 1 (403) в степени Blocker до выяснения причины
   на стороне владельца Google-аккаунта?
5. Добавлено при фиксе бага 5: запрещать ли прошлые даты заезда? Сейчас принимаются —
   в фикс не вошло, это продуктовое решение (в том числе про часовой пояс).
6. Там же: легальна ли бронь на 0 ночей (заезд = отъезд)? Сейчас проходит валидацию.

### Окружение

- **Клики через Browser MCP не работают:** `computer left_click` → таймаут 30 с,
  «The Browser pane is currently hidden». Интерактивные шаги делаются через Playwright.
- **Причина 403 у боевого Google Apps Script не установлена.** Проверено с одной машины
  и одного IP; отличить отозванное развёртывание от настройки доступа или ограничения Google
  из этого окружения нельзя. Нужна проверка владельцем аккаунта.
- `gh` CLI недоступен — pull request'ы не создаются, изменения идут прямо в `main`.
- `sessions/session-2.md` и `session-3.md` на ветке `main` отсутствуют — материалы сессий 2 и 3
  остались на ветке `naive-run` (коммит `fd7c418`).
- Поведение сайта на боевом GitHub Pages не проверялось ни разу — только локальный
  `python -m http.server` на Windows.

## Следующий шаг

Отметить в `qa/bug-reports.md`, что баг 5 исправлен, со ссылкой на `qa/retest-bug-05.md` —
сейчас отчёт о багах и отчёт о фиксе живут порознь. Там же закрыть две дыры покрытия
из раздела «Не начато»: проверка поля телефона (её надо сперва добавить в тест-план
и прогнать) и опечатка на `index-eng.html`.

Затем находка A-1: либо восстановить список расхождений, либо признать материал
утраченным. За ней A-2 и A-3 — сохранить лог ресурсов по 10 страницам и свести таблицу
pass/fail по TC-01…TC-12. Параллельно развести скриншоты по каталогам прогонов (B-6),
пока очередной `npx playwright test` не затёр доказательства: сейчас полный прогон
приходится делать на копиях в отдельном каталоге.

Дальше — ответить на шесть открытых вопросов и решить, чинить ли остальные 8 багов.
Каждый фикс — **новая сессия** (одна задача — одна сессия), с повторным прогоном того же
чек-листа, как сделано для бага 5. Отдельно стоит закрыть непокрытый сценарий 10 (клавиатура).

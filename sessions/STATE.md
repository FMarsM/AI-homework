# Состояние проекта

> Обновляется в конце каждой сессии. Описывает «как есть сейчас», а не историю
> изменений — старое перезаписывается. Первым делом в новой сессии агент читает
> именно этот файл.

**Обновлено:** `2026-08-16, сессия 2`

## Готово

- `SPEC.md` написан по итогам диалога с агентом: периметр QA-тестирования
  сайта `fmarsm.github.io-main`, сценарии (в т.ч. негативные/граничные),
  состояния, допущения, открытые вопросы, критерии приёмки.
- `AGENTS.md`, Часть II заполнена: что за проект, стек и версии, команды
  запуска, категоричные запреты, когда останавливаться и спрашивать,
  чек-лист перед «готово», формат баг-репорта, соглашения о структуре `qa/`.
- Репозиторий инициализирован (`git init`) и подключён к GitHub —
  [github.com/FMarsM/AI-homework](https://github.com/FMarsM/AI-homework),
  ветка `main`.
- `sessions/session-1.md` завершена (контекст → спецификация).
- **Сессия 2 — наивный прогон QA завершён.** Одним коротким промптом (без
  ссылки на `SPEC.md`/`AGENTS.md`, файлы отправлены в `1st-attempt/`, а не в
  `qa/`, — это сделано намеренно, для сравнения с будущим прогоном по полной
  спецификации):
  - `1st-attempt/test-plan.md` — 13 проверок, из них не менее трети
    негативные/граничные. Написан **после** прогона, а не до — отклонение от
    `AGENTS.md`, зафиксировано в самом файле и в `sessions/session-2.md`.
  - `1st-attempt/bug-reports.md` — 3 подтверждённых бага:
    1. **Major** — поле «Количество заселяющихся» (`contacts.html`,
       `Number_of_clients`) не валидирует нечисловой ввод (`type="text"` +
       сравнение строки с числом даёт `NaN`, `handleChange()` не срабатывает).
    2. **Minor** — все 7 полей формы на `contacts.html` используют один и тот
       же `id="text"` (дубли ID, невалидный HTML), у `<label>` нет `for` —
       подписи не связаны с полями.
    3. **Trivial** — ссылка «Контакты» в футере `contacts.html` (RU) ведёт на
       `#` вместо `contacts.html`, в отличие от всех остальных 9 страниц.
    Плюс отдельно задокументировано **опровержение** прежнего подозрения из
    `SPEC.md`: обратные слэши в путях ресурсов (`assets\img\...`) не ломают
    загрузку — браузеры нормализуют `\` в `/` для схемы `http(s)`; проверено
    `fetch()`-обходом всех ресурсов на всех 10 страницах, 404 не найдено.
  - `1st-attempt/tests/guest-count.spec.js` — Playwright-автотест на Баг #1:
    2 контрольных случая проходят, целевой (нечисловой ввод) реально падает.
    Запуск показан: `npx playwright test guest-count.spec.js --reporter=list`
    → `2 passed`, `1 failed` с трейсом.
  - Известное ограничение сессии: скриншоты не удалось сделать —
    `computer`/`screenshot` в этой среде стабильно возвращал ошибку
    `the Browser pane is not displayed`. Подтверждения багов — через
    DOM/JS-логи (`javascript_tool`, `read_page`, `read_network_requests`).

## В работе

- `sessions/session-1.md` — таблица «Использованные инструменты» всё ещё не
  заполнена (перенесено из предыдущей записи, не трогалось в сессии 2, т.к.
  чужие завершённые сессии задним числом не переписываются).

## Не начато

- «Полноценный» прогон по `SPEC.md`/`AGENTS.md` с сохранением в `qa/`
  (а не в `1st-attempt/`): ручной прогон на всех четырёх ширинах
  (375/768/1024/1440) со скриншотом на каждую, полный набор из ~13–20
  сценариев SPEC.md (клавиатурная навигация, слайдер по точкам/стрелкам,
  реальная тестовая отправка формы с меткой в ФИО).
  - Кандидат для доследования, найденный в сессии 2, но не подтверждённый
    визуально: при открытом мобильном меню и ресайзе окна до десктопной
    ширины без клика инлайн-стиль `display:flex` не сбрасывается (нет
    обработчика `resize`) — эффект на верстку не подтверждён без скриншота.
- Независимый аудит (вторая модель / чистая сессия / отдельный агент-ревьюер).
- Фикс хотя бы одного бага (в отдельной сессии/запросе) и повторный прогон
  тем же чек-листом.
- Причина, по которой скриншоты не работают в Browser MCP в этой среде, — не
  диагностировалась глубоко (проверялись только `resize_window`/`wait` как
  быстрые обходные пути, не помогло).

## Активные инструменты

- Python 3.13.2 — локальный сервер (`python -m http.server`), в системе.
- Git — коммиты/пуш в GitHub, `FMarsM/AI-homework`, ветка `main`.
- Встроенный Browser MCP — использован в сессии 2 для ручного QA
  (`navigate`, `read_page`, `read_console_messages`, `read_network_requests`,
  `javascript_tool`); `computer`/`screenshot` в этой среде не работает
  (см. TOOLS.md).
- **Node.js v24.19.0 (LTS)** — установлен в сессии 2 через `winget`
  (`OpenJS.NodeJS.LTS`), ранее отсутствовал.
- **`@playwright/test` + Chromium (Chrome for Testing 151.0.7922.34)** —
  установлены в сессии 2 в `1st-attempt/tests/`, использованы для автотеста.
- `gh` CLI — по-прежнему не установлен, PR не создавались, коммиты идут
  прямо в `main`.

## Известные проблемы

- `gh` CLI недоступен — `gh pr create` невозможен без установки.
- `sessions/session-1.md`: не заполнена таблица инструментов (см. «В работе»).
- Browser MCP `screenshot` не работает в этой среде (см. TOOLS.md, сессия 2)
  — все дальнейшие QA-сессии должны либо диагностировать причину, либо
  заранее закладывать DOM/JS-логи как основной формат подтверждения.
- `1st-attempt/` и `qa/` — два разных места для QA-артефактов: `1st-attempt/`
  это результат наивного прогона сессии 2 (по прямому указанию промпта), `qa/`
  зарезервирована `AGENTS.md` для полноценного прогона по `SPEC.md`. Не путать
  и не сливать при следующем прогоне без явного решения пользователя.

## Следующий шаг

Провести полноценный прогон по `SPEC.md`/`AGENTS.md` в новой сессии: тест-план
и результаты — в `qa/` (не в `1st-attempt/`), на всех четырёх ширинах со
скриншотами (сначала разобраться, почему `screenshot` не работал в сессии 2),
с полным набором сценариев, включая тестовую отправку формы (до 10 штук,
с меткой в ФИО) и повторную проверку после фикса хотя бы одного из трёх
найденных багов.
